{% extends 'base.html.twig' %}

{% block title %}Comercios{% endblock %}

{% block body %}
    <div class="mt-5"></div>
    <div class="table-responsive">
    <table class="table align-middle mb-0 bg-white">
        <thead class="bg-light">
        <tr>
            <th></th>
            <th>Id</th>
            <th>Email</th>
            <th>Nombre</th>
            <th>Comercios</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        <tr class="addCategoria">
            <td></td>
            <td></td>
            <td>Añadir uno nueva</td>
            <td></td>
            <td></td>
            <td>
                <button type="button" class="btn btn-link btn-sm btn-rounded">
                    <a href="/register/admin"><i class="fa-regular fa-square-plus fa-2x green"></i></a>
                </button>
            </td>
        </tr>
        {% for usuario in usuarios %}
            <tr>
                <td></td>
                <td>
                    <div class="d-flex align-items-center">
                    <p class="mb-1">{{ usuario.id }}</p>
                    </div>
                </td>
                <td>
                    <div class="d-flex align-items-center">
                        <p class="mb-1">{{ usuario.email }}</p>
                    </div>
                </td>
                <td>
                    <div class="d-flex align-items-center">
                        <p class="fw-bold mb-1">{{ usuario.nombre }}</p>
                        <p class="text-muted mb-0"></p>
                    </div>
                </td>
                <td>
                    <div class="d-flex align-items-center">
                    <p class="mb-1 ms-3">{{ numero[usuario.nombre] }}</p>
                    </div>
                </td>
                <td>
                    <button type="button" class="btn btn-link btn-sm btn-rounded">
                        <a href="/admin/usuario/{{ usuario.id }}/edit"><i class="far fa-pen-to-square fa-2x"></i></a>
                    </button>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
{% endblock %}