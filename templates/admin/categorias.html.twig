{% extends 'base.html.twig' %}

{% block title %}Comercios{% endblock %}

{% block body %}
    <div class="mt-5"></div>
    <div class="table-responsive">
    <table class="table align-middle mb-0 bg-white">
        <thead class="bg-light">
        <tr>
            <th></th>
            <th>Icono</th>
            <th>Nombre</th>
            <th>Comercios</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        <tr class="addCategoria">
            <td></td>
            <td></td>
            <td>Añadir una nueva</td>
            <td></td>
            <td>
                <button type="button" class="btn btn-link btn-sm btn-rounded">
                    <a href="/admin/categoria/new"><i class="fa-regular fa-square-plus fa-2x" style="color: green"></i></a>
                </button>
            </td>
        </tr>
        {% for categoria in categorias %}
            <tr>
                <td></td>
                <td>
                    <div class="d-flex align-items-center">
                        <img class="icono-50" src={{ asset(categoria.icono) }} alt="icono">
                    </div>
                </td>
                <td>
                    <div class="ms-3">
                        <p class="fw-bold mb-1">{{ categoria.nombre }}</p>
                        <p class="text-muted mb-0"></p>
                    </div>
                </td>
                <td>
                    <p>{{ numero[categoria.nombre] }}</p>
                </td>
                <td>
                    <button type="button" class="btn btn-link btn-sm btn-rounded">
                        <a href="/admin/categoria/{{ categoria.id }}/edit"><i class="far fa-pen-to-square fa-2x"></i></a>
                    </button>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
{% endblock %}